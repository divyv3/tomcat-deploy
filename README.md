# test-java

Simple Maven Spring Boot app that returns "heelow world" at the root URL.

Build and run:

```bash
mvn package
java -jar target/test-java-0.0.1-SNAPSHOT.jar
```

Or run with Maven:

```bash
mvn spring-boot:run
```

Then open http://localhost:8080 in your browser.
